services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=postgres'
      - 'POSTGRES_PASSWORD=secretpassword'
      - 'POSTGRES_USER=secretUser'
    ports:
      - '5432:5432'
